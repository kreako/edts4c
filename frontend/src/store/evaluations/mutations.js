export function setLoading (state, loading) {
  state.loading = loading
}

export function setEvaluations (state, evaluations) {
  state.evaluations = evaluations
}

export function setEvaluationDate (state, evaluationDate) {
  state.evaluationDate = evaluationDate
}

export function setStats (state, stats) {
  state.stats = stats
}
